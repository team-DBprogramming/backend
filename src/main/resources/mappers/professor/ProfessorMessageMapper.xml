<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.ProfessorMessageMapper">
  <select id="sendProfessorMessage" statementType="CALLABLE" parameterType="map">
    { CALL SEND_PROFESSOR_MESSAGE(
      #{professorUserId, mode=IN, jdbcType=NUMERIC},
      #{courseId, mode=IN, jdbcType=VARCHAR},
      #{division, mode=IN, jdbcType=VARCHAR},
      #{studentIds, mode=IN, jdbcType=VARCHAR},
      #{message, mode=IN, jdbcType=CLOB},
      #{result, mode=OUT, jdbcType=VARCHAR},
      #{sentCount, mode=OUT, jdbcType=NUMERIC}
    ) }
  </select>
</mapper>
