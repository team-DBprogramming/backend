<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.RefreshTokenMapper">
  <select id="callSaveLoginSuccess" statementType="CALLABLE" parameterType="map">
    { call SAVE_LOGIN_SUCCESS(
      #{userId, mode=IN, jdbcType=NUMERIC},
      #{tokenHash, mode=IN, jdbcType=VARCHAR},
      #{rememberMe, mode=IN, jdbcType=NUMERIC},
      #{expiresAt, mode=IN, jdbcType=TIMESTAMP},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>

  <select id="callRevokeRefreshToken" statementType="CALLABLE" parameterType="map">
    { call REVOKE_REFRESH_TOKEN(
      #{tokenHash, mode=IN, jdbcType=VARCHAR},
      #{loginId, mode=IN, jdbcType=VARCHAR},
      #{loginType, mode=IN, jdbcType=VARCHAR},
      #{revokedAt, mode=IN, jdbcType=TIMESTAMP},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>

  <select id="callValidateRefreshTokenActive" statementType="CALLABLE" parameterType="map">
    { call VALIDATE_REFRESH_TOKEN_ACTIVE(
      #{tokenHash, mode=IN, jdbcType=VARCHAR},
      #{loginId, mode=IN, jdbcType=VARCHAR},
      #{loginType, mode=IN, jdbcType=VARCHAR},
      #{checkedAt, mode=IN, jdbcType=TIMESTAMP},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>
</mapper>
