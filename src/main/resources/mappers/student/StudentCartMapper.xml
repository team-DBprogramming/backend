<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.StudentCartMapper">
  <resultMap id="CartItemResultMap" type="com.example.backend.dto.student.StudentCartItem">
    <result property="cartItemId" column="cart_item_id" />
    <result property="courseId" column="course_id" />
    <result property="courseName" column="course_name" />
    <result property="professor" column="professor" />
    <result property="division" column="division" />
    <result property="credit" column="credit" />
    <result property="schedule" column="schedule" />
    <result property="room" column="room" />
    <result property="seatStatus" column="seat_status" />
    <result property="conflictWith" column="conflict_with" />
    <result property="isApplicable" column="is_applicable" />
  </resultMap>

  <resultMap id="BulkEnrollResultMap" type="java.util.HashMap">
    <result property="cartItemId" column="cart_item_id" />
    <result property="success" column="success" />
    <result property="message" column="message" />
    <result property="code" column="code" />
  </resultMap>

  <resultMap id="BulkEnrollSummaryMap" type="java.util.HashMap">
    <result property="total" column="total" />
    <result property="success" column="success" />
    <result property="failed" column="failed" />
  </resultMap>

  <select id="findSectionId" resultType="long">
    SELECT c_no
    FROM class
    WHERE c_id = #{courseId}
      AND c_status != 'CANCELLED'
      AND (#{division} IS NULL OR TO_CHAR(c_no) = #{division})
    ORDER BY c_no
    FETCH FIRST 1 ROW ONLY
  </select>

  <select id="findCartItems" resultMap="CartItemResultMap">
    SELECT
      ci.cart_id AS cart_item_id,
      ci.c_id AS course_id,
      l.subject AS course_name,
      COALESCE(p.p_name, l.prof) AS professor,
      TO_CHAR(ci.c_no) || '분반' AS division,
      l.credit,
      LISTAGG(ct.c_day || ' ' || ct.c_start || '-' || ct.c_end, ', ')
        WITHIN GROUP (
          ORDER BY
            CASE ct.c_day
              WHEN 'MON' THEN 1
              WHEN 'TUE' THEN 2
              WHEN 'WED' THEN 3
              WHEN 'THU' THEN 4
              WHEN 'FRI' THEN 5
              WHEN 'SAT' THEN 6
              ELSE 7
            END,
            ct.c_start
        ) AS schedule,
      c.c_where AS room,
      CASE
        WHEN c.c_now >= c.c_max THEN 'FULL'
        WHEN c.c_max - c.c_now <![CDATA[<=]]> 5 THEN 'CLOSING_SOON'
        ELSE 'AVAILABLE'
      END AS seat_status,
      NULL AS conflict_with,
      CASE WHEN c.c_now <![CDATA[<]]> c.c_max THEN 1 ELSE 0 END AS is_applicable
    FROM cart_item ci
    JOIN class c ON c.c_id = ci.c_id AND c.c_no = ci.c_no
    JOIN lecture l ON l.no = c.c_id
    LEFT JOIN professor p ON p.p_id = COALESCE(c.p_id, l.p_id)
    LEFT JOIN class_time ct ON ct.c_id = c.c_id AND ct.c_no = c.c_no
    WHERE ci.s_id = #{studentId}
    GROUP BY
      ci.cart_id,
      ci.c_id,
      l.subject,
      COALESCE(p.p_name, l.prof),
      ci.c_no,
      l.credit,
      c.c_where,
      c.c_now,
      c.c_max,
      ci.added_at
    ORDER BY ci.added_at DESC
  </select>

  <select id="callInsertCart" statementType="CALLABLE" parameterType="map">
    { CALL INSERT_CART(
      #{studentId, mode=IN, jdbcType=VARCHAR},
      #{courseId, mode=IN, jdbcType=VARCHAR},
      #{sectionId, mode=IN, jdbcType=NUMERIC},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>

  <select id="callDeleteCart" statementType="CALLABLE" parameterType="map">
    { CALL DELETE_CART(
      #{cartItemId, mode=IN, jdbcType=NUMERIC},
      #{studentId, mode=IN, jdbcType=VARCHAR},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>

  <select id="callDeleteCartBySection" statementType="CALLABLE" parameterType="map">
    { CALL DELETE_CART_BY_SECTION(
      #{studentId, mode=IN, jdbcType=VARCHAR},
      #{courseId, mode=IN, jdbcType=VARCHAR},
      #{sectionId, mode=IN, jdbcType=NUMERIC},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>

  <select id="callCountCart" statementType="CALLABLE" parameterType="map">
    { CALL COUNT_CART(
      #{studentId, mode=IN, jdbcType=VARCHAR},
      #{count, mode=OUT, jdbcType=NUMERIC}
    ) }
  </select>

  <select id="callEnrollFromCart" statementType="CALLABLE" parameterType="map">
    { CALL ENROLL_FROM_CART(
      #{studentId, mode=IN, jdbcType=VARCHAR},
      #{cartItemIdsCsv, mode=IN, jdbcType=VARCHAR},
      #{results, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=com.example.backend.mapper.StudentCartMapper.BulkEnrollResultMap},
      #{summary, mode=OUT, jdbcType=CURSOR, javaType=java.sql.ResultSet, resultMap=com.example.backend.mapper.StudentCartMapper.BulkEnrollSummaryMap}
    ) }
  </select>

  <select id="findCartId" resultType="long">
    SELECT cart_id
    FROM cart_item
    WHERE s_id = #{studentId}
      AND c_id = #{courseId}
      AND c_no = #{sectionId}
    ORDER BY added_at DESC
    FETCH FIRST 1 ROW ONLY
  </select>

</mapper>
