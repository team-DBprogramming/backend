<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.RefreshTokenMapper">
  <select id="callSaveLoginSuccess" statementType="CALLABLE" parameterType="map">
    { call SAVE_LOGIN_SUCCESS(
      #{userId, mode=IN, jdbcType=NUMERIC},
      #{tokenHash, mode=IN, jdbcType=VARCHAR},
      #{rememberMe, mode=IN, jdbcType=NUMERIC},
      #{expiresAt, mode=IN, jdbcType=TIMESTAMP},
      #{result, mode=OUT, jdbcType=VARCHAR}
    ) }
  </select>

  <select id="countActive" resultType="int">
    SELECT COUNT(*)
    FROM refresh_token
    WHERE token_hash = #{tokenHash}
      AND revoked_at IS NULL
      AND expires_at &gt; #{now}
  </select>

  <update id="revoke">
    UPDATE refresh_token
    SET revoked_at = #{revokedAt}
    WHERE token_hash = #{tokenHash}
      AND revoked_at IS NULL
  </update>
</mapper>
