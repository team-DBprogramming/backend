<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.mapper.RefreshTokenMapper">
  <insert id="insert">
    INSERT INTO refresh_token (user_id, token_hash, remember_me, expires_at)
    VALUES (#{userId}, #{tokenHash}, #{rememberMe}, #{expiresAt})
  </insert>

  <select id="countActive" resultType="int">
    SELECT COUNT(*)
    FROM refresh_token
    WHERE token_hash = #{tokenHash}
      AND revoked_at IS NULL
      AND expires_at &gt; #{now}
  </select>

  <update id="revoke">
    UPDATE refresh_token
    SET revoked_at = #{revokedAt}
    WHERE token_hash = #{tokenHash}
      AND revoked_at IS NULL
  </update>
</mapper>
